# Step 02 · shadcn/ui

**Status:** queued — awaiting Step 01 merge.
**Reference:** `docs/design/Grade Sight Handoff v2.html` §Step 02 (Implementation).
**Branch when ready:** `step-02-shadcn-ui` (per `feedback_workflow.md` memory).

## Init

```bash
pnpm dlx shadcn@latest init
```

- `baseColor: neutral`
- `cssVars: yes`

## Components to add (exact, no extras)

```bash
pnpm dlx shadcn@latest add button card alert skeleton badge dialog dropdown-menu separator sonner avatar
```

Ten primitives. Do not add others — later steps will add as they're needed.

## `components/ui/button.tsx` overrides

| Variant   | Classes |
|-----------|---------|
| Primary   | `bg-ink text-paper hover:bg-black` |
| Secondary | `border border-rule bg-paper hover:bg-paper-soft text-ink` |
| Ghost     | `text-accent hover:bg-accent/10` |

- Radius: `rounded-[3px]` everywhere (overrides shadcn's default `rounded-md`).
- Strip any gradient/shadow defaults that ship in the generated source.
- Do NOT use shadcn's `bg-primary text-primary-foreground` aliases — we want explicit token names.

## `components/ui/card.tsx` overrides

```
bg-paper border-rule rounded-[3px] shadow-none
```

No shadows on cards, ever.

## Verification gate (must pass before opening the Step 02 PR)

1. Mount one of each primitive on a throwaway `/dev/primitives` route.
2. Screenshot the rendered page.
3. Confirm paper/ink palette only — no `rounded-md`, no shadows, no `slate-*` leakage from shadcn defaults.

**Do not start Step 03 until that screenshot passes.**

## PR description (use the six-heading template from `feedback_workflow.md`)

- **Step:** 02 — shadcn/ui
- **What:** init shadcn, add the 10 primitives above, rewrite `button.tsx` + `card.tsx` to map onto v2 paper/ink tokens
- **Tokens used:** `--color-ink`, `--color-paper`, `--color-paper-soft`, `--color-rule`, `--color-accent`; `rounded-[3px]` baked inline (matches `--radius-sm`). No raw hex; no `bg-primary` aliases.
- **Verification:** screenshot of `/dev/primitives` mounting all 10
- **Seven-item checklist:** confirm pass (handoff doc, "Before merging any PR")
- **Open questions / provisional decisions:** none expected; flag any deviations shadcn injects during init that we have to keep
