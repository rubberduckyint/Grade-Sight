// TODO(contact-form): swap this in-page mailto for the /contact route's
// server-side handler once that surface ships. Until then, exposing the
// address client-side is the trade for a working "Tell us" link today.
export const SUPPORT_EMAIL = "support@gradesight.ai";
